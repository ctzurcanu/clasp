;; Tests for posix.lisp

(load "clisp/kernel/lsp/posix.lisp")

(print "posix.lisp loaded successfully!")

;; Test that exported symbols are accessible
;; Since the package system is stubbed, we just verify it loads

(print "All posix.lisp tests passed!")
